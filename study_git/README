git是一个版本管理系统，对于团队协作很关键，所以我们必须要学。

git是一个版本控制工具，而诸如阿里云code、bitbucket、github则是一个用git做版本控制的项目托管平台。
大概就是「魔兽争霸」与「对战平台」的关系。 

Thomas使用的bitbucket，但是bitbucket只能创建５个私有仓库，而且分享的人数有限制，所以我们的团队选择的是阿里云code。阿里云code没有这些限制，且服务器在中国，传输的速度也很快。

在这个git_reference的仓库中，我们会介绍阿里云code和bitbucket的使用方法。
